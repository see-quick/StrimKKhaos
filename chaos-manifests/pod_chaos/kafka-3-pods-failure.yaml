apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kafka-3-pods-failure-6
  namespace: myproject
spec:
  action: pod-failure
  mode: fixed
  selector:
    namespaces:
      - myproject
    labelSelectors:
      strimzi.io/kind: "Kafka"
      strimzi.io/broker-role: "true"
  value: "3"
  duration: "5m"
